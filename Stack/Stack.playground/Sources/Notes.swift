import Foundation

/*

 1. Index of particular element in array :
 firstIndex(of: element)

 2. Reverse the array
 arr.reverse()

 3. Reverse in loop
 for i in arr.reversed()

 4. Dict index access
 accessing the key of the dict [String: [Any]] at a particular index:
 Array(dict)[indexPath.section].key
 Array(dict)[indexPath.section].value
 */
